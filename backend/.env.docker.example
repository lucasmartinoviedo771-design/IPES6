# Ejemplo de configuración para Docker
# Copia este archivo a .env y ajusta los valores según tu entorno

# Django Settings
DJANGO_ENV=production
DEBUG=False
SECRET_KEY=django-insecure-CAMBIAR_ESTO_POR_UNA_CLAVE_SEGURA_ALEATORIA_DE_50_CARACTERES

# Database Configuration
DB_NAME=ipes6
DB_USER=ipes_user
DB_PASSWORD=CAMBIAR_ESTO_contraseña_segura_para_usuario
DB_ROOT_PASSWORD=CAMBIAR_ESTO_contraseña_segura_para_root
DB_HOST=db
DB_PORT=3306

# Network Configuration
# IMPORTANTE: Reemplaza con la IP o dominio de tu servidor
# Ejemplo con IP local: 192.168.1.100
# Ejemplo con dominio: midominio.com
ALLOWED_HOSTS=localhost,127.0.0.1,backend,TU_IP_O_DOMINIO_AQUI
FRONTEND_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,http://TU_IP_O_DOMINIO_AQUI
CSRF_TRUSTED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,http://TU_IP_O_DOMINIO_AQUI
FRONTEND_URL=http://localhost:8080

# Si usas HTTPS, cambia http por https en las URLs de arriba
# y descomenta la siguiente línea:
# SECURE_SSL_REDIRECT=True

# Email Configuration (Opcional)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu_email@gmail.com
# EMAIL_HOST_PASSWORD=tu_contraseña_o_app_password

# Media Files
MEDIA_URL=/media/
MEDIA_ROOT=/app/media

# Static Files
STATIC_URL=/static/
STATIC_ROOT=/app/staticfiles

# Logging
LOG_LEVEL=INFO

# Configuración adicional
LANGUAGE_CODE=es-ar
TIME_ZONE=America/Argentina/Buenos_Aires
USE_I18N=True
USE_TZ=True
